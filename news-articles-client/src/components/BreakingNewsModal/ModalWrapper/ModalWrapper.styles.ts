import styled from "styled-components";

export const ModalWrapperStyles = styled.div.attrs({className: 'ModalWrapperStyles'})`
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
