import styled from "styled-components";

export const ModalErrorMessageStyles = styled.div.attrs({className: 'ModalErrorMessageStyles'})`
  color: black;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
